# CineRec — 작업 인수인계 메모

새 Claude 세션이 이 프로젝트를 이어받을 때 먼저 읽는 파일입니다.
(사용자에게 보여주는 문서가 아니라, 다음 세션이 맥락을 빨리 잡기 위한 내부 메모예요.)

## 프로젝트가 뭔지

**CineRec** — 리뷰 기반 영화 추천 웹사이트. 사용자가 영화를 검색 → 별점·리뷰 남기고 →
9개 항목(연출/각본/독창성/주제/미장센/연기/장르/편집/음악) 세부 평가 → (선택) 장르·러닝타임·
영화 유형 필터 → TMDB Discover API로 실시간 추천을 받는 5단계 플로우.

**핵심 원칙(사용자가 여러 번 강조함): 영화 데이터를 코드에 절대 하드코딩하지 않는다.**
전부 TMDB API를 실시간 호출해서 가져온다. 이 원칙은 앞으로도 지켜야 함.

## 파일 구조

```
index.html          — 마크업만
style.css            — 전체 스타일 (다크 모드 + 네온 블루 #00d2ff 테마)
script.js            — 앱 로직, i18n(한/영), TMDB 연동, 추천 알고리즘
api/tmdb.js          — Vercel 서버리스: TMDB 프록시 (키 서버 은닉)
api/save-review.js   — 사용자 리뷰를 Upstash Redis에 저장
api/list-reviews.js  — 관리자용 리뷰 조회 (세션 쿠키 인증)
api/admin-login.js   — 관리자 로그인 (세션 쿠키 발급 + 브루트포스 방지)
api/admin-logout.js  — 세션 무효화
admin.html           — 관리자 전용 페이지 (같은 사이트의 /admin.html 경로, 비공개)
netlify/functions/tmdb.js — Netlify용 TMDB 프록시 (실제로는 안 씀, Vercel만 사용 중)
ticket.png            — 사용자가 직접 만든 인트로 티켓 이미지 (진짜 알파 투명 배경)
intro-bg.svg          — 직접 그린 영화관 좌석 배경 (자체 제작 SVG, 스톡 이미지 아님)
tmdb-logo.svg         — 사용자가 첨부한 TMDB 공식 로고 (footer 표기용)
```

## 배포 상태

- **GitHub 저장소**: `https://github.com/iamcaesarea-sketch/260824_2` (사용자가 GitHub 웹 UI로 직접 파일을 업로드하는 방식. 로컬 git push 인증 수단이 없어서 CLI push는 못 함 — 항상 "다시 올려야 할 파일 목록"을 알려주는 방식으로 안내해왔음)
- **Vercel**: `https://cinereccc.vercel.app` 에 배포됨, GitHub 연동으로 자동 재배포
- **로컬에는 git 저장소가 있고 커밋도 쌓여있지만, 원격에 실제로 반영되는 건 사용자가 GitHub 웹에서 파일을 업로드해야만 함.** 로컬 `git log`가 실제 배포 상태를 보장하지 않음 — 확인하려면 사용자에게 물어보거나 `curl https://cinereccc.vercel.app/...`로 직접 라이브 사이트를 확인할 것.

### 필요한 환경변수 (Vercel 프로젝트 Settings → Environment Variables)
- `TMDB_API_KEY` — 등록 완료된 것으로 보임 (사이트가 정상 작동 중이었음)
- `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` — 사용자가 Upstash 가입 후 Redis DB "Cinerec" 생성함 (Tokyo 리전). **이 값들을 실제로 Vercel에 등록했는지, admin.html이 실제로 작동하는지는 이 대화 마지막까지 확인 안 됨 — 다음 세션에서 제일 먼저 확인할 것.**
- `ADMIN_PASSWORD` — 등록 여부 미확인

## 이번 세션에서 만든 주요 기능 (최신 커밋 기준)

1. **Pretendard 폰트, 다크+네온블루 테마, 한/영 i18n, 반개 별점** (초기)
2. **인트로 화면** — 직접 그린 영화관 좌석 SVG 배경 + 사용자가 만든 티켓 이미지(`ticket.png`) 클릭 → 앱 진입, 가장자리 은은한 글로우 애니메이션
3. **5단계 플로우**: 01 영화 선택 → 02 별점·리뷰 → 03 세부 평가 → **04 추가 선택(신규)** → 05 추천 결과
   - 04단계: 장르 다중 선택(칩), 러닝타임(1h30m/2h/상관없음), 영화 유형(상업/독립/단편/상관없음), 건너뛰기 버튼
   - 독립/단편 필터는 `/search/keyword`로 TMDB 키워드 ID를 **그때그때 실시간 조회**해서 사용 (하드코딩 금지 원칙 지킴)
4. **추천 이유에 "비슷한 별점 사용자" 신호** — 별점이 0.5/1/4/4.5/5일 때 `/movie/{id}/recommendations` 결과를 섞어서 반영
5. **IMDB 링크 버그 수정** — 추천작마다 `/movie/{id}/external_ids`로 실제 imdb_id를 받아와 정확한 링크 연결 (예전엔 전부 검색 링크였음)
6. **한줄 플롯 요약** 추가, 포스터 비율 버그(가로 잘림) 수정, 카드 레이아웃 개편(포스터 아래부터 풀와이드 본문)
7. **사용자 데이터 수집** — `api/save-review.js`로 리뷰를 Upstash Redis에 저장 → `admin.html`에서 조회
   - 처음엔 비밀번호를 URL 쿼리로 보내는 방식이었는데, 사용자가 "더 견고하게(근데 무료로)" 요청해서 **세션 쿠키(HttpOnly) + 브루트포스 방지(5회 실패시 15분 잠금)** 방식으로 업그레이드함
8. **"내가 추천받은 영화" 갤러리** — 로그인 없이 `localStorage`에 추천받은 영화를 전부 모아서 장르별 탭으로 필터링해 보여줌 (기존 "내 리뷰 기록"과는 별개 섹션)
9. **TMDB/JustWatch 저작권 표기** — footer에 TMDB 로고 + 공식 문구, JustWatch 텍스트 출처 표기
10. **Toss UX 라이팅 가이드** 적용해서 한국어 문구 전반 검수(해요체 통일, 긍정형 표현 등)

## 2026-08-25 세션에서 반영한 요청사항 (style.css?v=17, script.js?v=10)

1. 추천 결과 줄거리(plot) 폰트 13.5px→12px 축소
2. 04단계 장르 칩에 "재난" 추가 — TMDB에 전용 장르가 없어서 독립/단편처럼 `/search/keyword`로 "disaster" 키워드 ID를 실시간 조회해서 근사 (하드코딩 아님)
3. 추천 영화 카드에 러닝타임 표기 (연도 옆에 "· 2시간 7분" 형식). 이 과정에서 연도+러닝타임 텍스트가 길어져 좁은 화면에서 제목이 세로로 깨지는 버그가 생겨서 `.ticket .head .title-row`를 flex-wrap으로 수정함
4. 스텝 탭(01~05) 이모지 전부 제거, `.steps` CSS를 overflow-x:auto 스크롤 방식에서 flex-wrap 방식으로 변경해 가로 스크롤 없이 한눈에 보이게 함
5. 관리자 페이지(admin.html)에 "선택 필터" 컬럼 추가 — 04단계에서 고른 장르/러닝타임/개봉연도/영화유형을 `api/save-review.js`가 `filters` 필드로 같이 저장하도록 확장
6. 헤더 태그라인 문구 축약("실시간으로" 제거) + 명시적 줄바꿈 위치 조정, 폰트 15px→13px(모바일 12px)로 줄여서 모바일에서 어중간하게 안 잘리도록 수정
7. 04단계에 "개봉연도" 필터 추가 (2000년대/2010년대/2020년대/상관없어요 4개, TMDB `primary_release_date.gte/lte`로 연대 범위 조회)
8. 결과 화면 하단 disclaimer에서 "(코드에 미리 저장된 영화 목록은 없어요)" 문구 제거
9. "📝 추천 받았던 영화" 버튼을 사이트 맨 밑(히스토리 아래)에 새로 만들고, 클릭 시 패널이 열리며 "날짜별로 보기"/"장르별로 보기" 정렬 옵션 제공. 각 카드에 추천 이유(reasons)도 함께 표시하도록 `savedRecs`에 reasons 필드 추가

이 세션에서도 로컬 테스트 서버(`local_server.py`, 스크래치패드)를 새로 만들어서 TMDB 실키로 전체 플로우(검색→선택→평가→04단계 필터→추천→관리자 페이지)를 끝까지 확인함. 다음 세션 스크래치패드는 사라지므로 필요하면 다시 만들어야 함.

## 2026-08-25 세션 2차 요청 반영 (style.css?v=18, script.js?v=11)

1. footer에 contact 이메일(`iamcaesarea@gmail.com`, mailto 링크) 추가
2. 추천 결과 티켓 레이아웃 변경 — 연도(+러닝타임)를 맨 위에, 영화 제목을 그 아래 풀와이드 한 줄로. (1차 세션에서 title-row를 flex로 나란히 배치했더니 좁은 화면에서 제목이 세로로 깨지는 문제가 있었는데, 아예 세로 스택 구조로 바꿔 근본 해결)
3. 추천작마다 "저장하기" 버튼 추가 — **더 이상 추천 결과가 자동으로 갤러리에 저장되지 않음.** 클릭해야만 `saveRecToGallery()` 호출됨 (기존엔 `renderResult`에서 4개 전부 자동 저장했었음, 이제 사용자가 고른 것만)
4. "내 리뷰 기록"을 "추천 받았던 영화" 버튼 밑에 별도 토글 버튼(`historyBtn`)으로 분리 — `historyOpen` 상태로 열고 닫음 (recsGallery와 동일 패턴)
5. 01단계 검색창 밑 두 안내문구(`.search-hint`, `.empty-note`) 좌측 정렬/줄간격 통일 (`.empty-note`의 padding 제거)
6. 개봉연도 옵션을 "상관없어요 / 고전영화(1980년 이전) / 2000년대 / 2010년대 / 2020년대"로 재정리. `manual.decade`가 문자열 `'classic'`일 수 있음 — `computeTmdbRecommendations`, `api/save-review.js`, `admin.html`의 `filtersStr()` 전부 이 케이스를 특별 처리하도록 수정함 (Number(decade)로 그냥 캐스팅하면 NaN이 되므로 주의)
7. 스텝 탭(01~05) 강제로 한 줄 고정 — flex-wrap을 다시 nowrap으로 되돌리고, 모바일(`max-width:480px`)에서 폰트 9px + `.steps`에 `margin:-20px`로 풀블리드해서 좁은 화면에서도 절대 줄바꿈 안 되게 함
8. 세부 평가(03단계)에서 **독창성/편집/음악** 3개를 04단계로 이동 — `STEP3_ASPECT_KEYS`(6개) / `EXTRA_ASPECT_KEYS`(3개)로 분리, `renderAspectSliders()` 공용 헬퍼로 양쪽 렌더링. `ASPECT_KEYS`(9개 전체)는 추천 로직(`getLikedDisliked` 등)에서 그대로 사용 — UI 위치만 바뀐 것이라 로직 변경 없음
9. 04단계 "상관없어요"류 버튼을 전부 각 그룹의 첫 번째 선택지로 이동 (러닝타임/개봉연도/영화유형)

로컬 서버로 전체 플로우 + admin.html 필터 컬럼(특히 `decade:'classic'` 케이스)까지 재확인 완료.

## 2026-08-25 세션 3차 요청 반영 (style.css?v=19, script.js?v=12)

1. 안내 문구 두 개 수정: `emptyNoteDefault`("영화는 TMDB 전체 카탈로그에서 실시간으로 검색 중이에요"), `statusOn`("서버 연동 켜짐 - 실시간 검색이 가능한 상태예요")
2. `.steps`(01~05 탭)를 `justify-content:center`에서 `space-between`으로 바꿔서 밑에 있는 `.panel` 박스와 가로폭이 정확히 맞도록 함. 모바일 폰트도 8.5px까지 더 줄임 (여전히 한 줄 고정 유지)
3. "05 추천 결과" → "05 결과"로 축약
4. "내 리뷰 기록"·"추천 받았던 영화" 각 항목에 삭제(×) 버튼 추가 — `.delete-x` 공용 스타일. 히스토리 항목엔 고유 `id`(Date.now())를 새로 부여해서 정확히 그 항목만 지울 수 있게 함. `rec-card`는 `<a>`라 그 안에 버튼을 못 넣어서 `.rec-card-wrap`(div)으로 한 번 더 감싸고 `×` 버튼을 절대 위치로 오버레이함
5. **admin.html 전면 개편** — 기존의 좁은 표(`<table>`) 방식을 버리고, 날짜별 탭(`#dateTabs`, `createdAt.slice(0,10)` 기준 그룹) + 리뷰 하나당 카드(`.review-card`) 형식으로 변경. 각 탭 안에서는 `createdAt` 기준 최신순 정렬. 표 컬럼을 줄맞출 필요가 없어져서 리뷰 텍스트·세부평가·필터를 각각 줄바꿈해서 훨씬 읽기 편해짐
6. 03단계 세부 평가 순서를 장르→연출→각본/대사→주제→미장센→연기로 재배열 (`STEP3_ASPECT_KEYS` 순서만 변경, 로직 영향 없음)
7. 04단계 "세부 평가 더 보기" 라벨 삭제하고, 독창성/편집/음악 슬라이더 그룹을 04단계 맨 밑(영화 유형 그룹 다음)으로 이동
8. "고전영화 (1980년 이전)" → "고전영화"로 버튼 라벨 축약 (필터링 로직·연도 범위는 그대로 1979-12-31 이전)

이번에도 로컬 서버로 전체 플로우 재확인 (특히 삭제 버튼은 JS로 상태값까지 직접 확인: 히스토리/갤러리 각각 항목 하나만 정확히 지워지는 것 검증). admin.html 카드 UI도 테스트 리뷰 여러 개 심어서 스크린샷으로 확인함.

## 2026-08-25 세션 4차 — TMDB 표기 정리 (script.js?v=13)

사용자가 "footer에 TMDB 로고+공식문구만 있으면 저작권 요건 충족되냐"고 물어봐서, TMDB API 이용약관상 **footer의 로고+"This product uses the TMDB API..." 문구가 필수 표기이고, 화면 중간중간의 "TMDB에서 검색 중" 같은 문구는 법적 요건이 아니라 그냥 UX 카피**라고 설명함. 그랬더니 "지금 거의 TMDB 자체 사이트 같다"며 나머지를 간결하게 정리해달라고 요청.

- footer 표기(로고 + 공식 영문 문구 + JustWatch 출처)는 **그대로 유지** — 이건 지우면 안 됨
- 그 외 화면 문구(검색 안내, 로딩 문구, 추천 이유, disclaimer, 영어판 tagline 등)에서 "TMDB" 브랜드명을 전부 빼고 일반적인 표현으로 순화 (예: "TMDB에서 검색 중…" → "검색 중…", "TMDB Discover 조건에 맞아 추천됐어요" → "설정하신 조건에 맞아 추천됐어요")
- 코드 내부 주석(`/* TMDB Search API... */` 등)은 개발자용이라 그대로 둠 — 사용자에게 안 보이는 부분

## 2026-08-25 세션 5차 (script.js?v=14)

1. 이모지 3개 추가: `emptyNoteDefault` 앞에 🔄, `searchHint` 앞에 💡(기존 "·" 불릿은 이모지로 대체), `reasonLabel`("추천 이유") 앞에 😎
2. 04단계 장르에 "추리" 추가 + 장르 칩 전체를 선호도(대중적으로 많이 찾는) 순으로 재정렬. "재난"과 마찬가지로 TMDB에 전용 장르가 없어서 `/search/keyword?query=detective` 키워드 ID로 근사함 (동일한 방식이라 기존 `DISASTER_GENRE_VALUE` 단일 상수를 `EXTRA_GENRE_CHIPS`(map) + `GENRE_RENDER_ORDER`(TMDB 장르ID/extra키 혼합 배열) 구조로 일반화해서, 앞으로 이런 "TMDB엔 없는 장르" 칩이 또 필요해지면 `EXTRA_GENRE_CHIPS`에 항목만 추가하면 되게 만들어둠). 추리는 미스터리 바로 뒤, 재난은 맨 끝에 배치

로컬 서버로 장르칩 순서·"추리" 선택 시 실제 discover 쿼리(`with_keywords`)까지 확인 완료.

## 2026-08-25 세션 6차 — 홈페이지 로고 이미지화 + 버그 2건 수정 (script.js?v=16)

1. 헤더의 텍스트 `<h1>CINEREC</h1>`를 사용자가 만든 `cinerec_logo_thin.png`(검정 로고, 투명배경) 이미지로 교체. 다크 테마라 검정 로고가 그대로면 안 보여서 `filter:invert(1)`로 흰색처럼 보이게 처리. 데스크톱(`min(40vw,480px)`)과 모바일(`max-width:520px`에서 `min(85vw,320px)`)을 다른 크기로 분리 — "웹으로 보면 작다"는 피드백 받고 데스크톱 사이즈만 더 키움
2. **버그 수정 — 삭제 시 여러 개가 지워지는 문제**: "추천 받았던 영화"/"내 리뷰 기록"의 삭제(×) 버튼이 `.onclick=` 방식이었는데, (이 세션에서 자체 테스트 중에도 실제로 목격한) 클릭 이벤트가 중복 발생하는 상황에서 같은 버튼이 여러 번 처리되는 게 원인으로 추정됨. `addEventListener(..., {once:true})`로 바꿔서 같은 버튼은 최초 1회만 처리되게 방어함 (버튼 3번 강제 클릭해도 1개만 삭제되는 것까지 테스트로 확인)
3. **버그 수정 — 두 패널이 동시에 열리거나 하나도 안 열리는 문제**: `recsGalleryBtn`/`historyBtn`이 서로 완전히 독립적으로 토글되던 구조라(애초에 아코디언 로직이 없었음) 하나를 열어도 다른 하나가 안 닫히는 게 정상 동작이었음 → 한쪽을 열면 반대쪽 상태를 강제로 `false`로 만들고 양쪽 다 `render`하도록 수정해서 아코디언처럼 항상 하나만 열리게 함

## 2026-08-25 세션 7차 — 듀오링고 스타일 참고 UI 리디자인 (style.css?v=24)

"UI가 너무 투박하다"는 피드백으로 style.css 전체를 다시 씀 (Write로 전체 교체, 클래스명/HTML/JS는 손 안 댐). 듀오링고를 "그대로 베끼지 말고 참고만" 해달라는 요청이라 다크+네온블루 브랜드 정체성(색상)은 그대로 두고, **인터랙션 방식**만 듀오링고식으로 바꿈:

1. **버튼 — "눌리는" 촉감**: `button.primary`/`button.ghost`에 `box-shadow:0 4px 0 <어두운색>`으로 아래쪽에 두꺼운 그림자 테두리를 주고, `:active`일 때 `translateY(4px)` + 그림자를 0으로 줄여서 실제로 눌리는 느낌 구현. `.chip`/`.genre-tab`/`.sort-btn` 같은 작은 알약형 버튼들은 이 효과를 안 줌(전부 다 누르면 너무 산만해서) — `button{}` 기본 규칙엔 box-shadow를 안 넣고 `.primary`/`.ghost`에서만 추가하는 방식으로 분리함
2. **스텝 탭(01~05) 완전 재설계**: 밑줄 텍스트 방식 → 5개짜리 알약형 진행 트레일로 변경 (`flex:1`로 균등분할이라 화면 폭에 따라 자동으로 딱 맞음 — 예전에 썼던 폰트크기/음수마진 계산 트릭이 필요 없어짐). done=연한 하늘색 테두리, active=꽉 찬 하늘색 채움+그림자
3. **전반적으로 border-radius 대폭 확대** (4~6px → 14~22px, `--radius-md`/`--radius-lg`/`--radius-pill` 변수로 통일), 테두리 굵기 1px→2px, 칩 선택 시 `chipPop` 바운스 애니메이션 추가
4. 인풋/텍스트에어리어 포커스 스타일을 얇은 outline → 컬러 링(box-shadow) 방식으로, 슬라이더 썸/트랙도 더 크게 키움

**admin.html은 안 건드림** — 별도 `<style>`을 쓰는 내부 관리자 페이지라 "홈페이지" 범위 밖으로 판단.

로컬 서버로 1~5단계 전체 플로우 스크린샷 확인. 이 세션에서 스크린샷 캡처가 여러 번 오래된 화면을 보여주는 현상이 있었는데(리사이즈/새로고침하면 해결됨), `getComputedStyle`로 직접 값 대조해서 실제로는 CSS가 정상 적용되고 있음을 재확인했음 — 다음 세션에서 비슷한 "스크린샷이 이상해요" 상황 만나면 캡처 오류부터 의심하고 DOM/computed style 확인해볼 것.

## 2026-08-25 세션 8차 (style.css?v=26, script.js?v=17)

1. **스텝 탭 재수정** — 지난 세션에서 알약형 버튼처럼 만들었더니 "탭 가능한 부분이 아닌데 버튼처럼 보인다"는 피드백. box-shadow(눌리는 효과)·배경 채움·테두리를 다 빼고 얇은 밑줄(`border-bottom`)만 남기는 방식으로 되돌림 — `flex:1` 균등분할 레이아웃은 그대로 유지
2. **포인트 컬러를 네온블루(#00d2ff) → 마룬(#800000)으로 전체 교체**. `--accent`/`--accent-dim`/`--accent-glow` 변수 값만 바꾸고 끝나지 않음 — 마룬은 시안보다 훨씬 어두운 색이라, 기존에 "밝은 accent 배경 위에 검정 텍스트(#04141c)"로 짜여있던 곳들(주 버튼, 언어 스위치 활성 탭 등)은 그대로 두면 텍스트가 안 보이게 돼서 전부 `var(--text)`(밝은색)로 바꿔줌. 칩/탭 선택 상태도 텍스트를 accent색 그대로 쓰면 어두운 배경 위에서 대비가 낮아져서(시안 대비 가독성 떨어짐) 배경 틴트는 마룬으로 유지하되 텍스트는 밝은색으로 바꾸는 식으로 손봄. `rgba(0,210,255,...)` 형태로 하드코딩돼 있던 값들(변수 안 쓰고 직접 박아둔 곳)도 전부 찾아서 마룬으로 변환함
3. **footer contact 형식 변경** — "Contact✉️ 이메일" → "✉️ 이메일"로 "Contact" 단어 제거, 색상도 accent 강조 없이 `--text-dim` 단색으로(호버 시 색 변화도 제거)
4. **세부 평가에 "몰입도" 항목 추가** — 03단계 핵심 그룹(`STEP3_ASPECT_KEYS`)에 7번째로 추가, 04단계 "더 보기"(독창성/편집/음악)는 그대로 3개 유지 → 전체 `ASPECT_KEYS` 9개→10개. `KEYWORDS`(리뷰 텍스트 키워드 감지)에도 `immersion: ['몰입','몰입감','빠져들','집중']` 추가
5. **"9가지 항목" 문구 전수 조사 → "10가지"로 수정** — 사용자가 "정확히 9개 맞냐" 물어봐서 확인해보니 몰입도 추가 전엔 정확히 9개였음(문제 없었음). 몰입도를 추가하면서 10개가 됐으니 태그라인(ko/en, index.html 정적 텍스트 포함)과 step3 안내문구(ko/en) 총 5곳을 전부 "10가지"/"10 aspects"로 동기화함

## 2026-08-25 세션 9차 — 글자색/도형색 분리 (style.css?v=27)

"글자 가독성이 너무 떨어지니 글자 색은 다른 걸로 바꾸고 도형들만 포인트 컬러(마룬) 유지해달라"는 요청. `color:var(--accent)` / `color:var(--accent-dim)`로 텍스트에 직접 마룬을 쓰던 곳을 전부 찾아서(`header .eyebrow`, 스텝 탭 활성/완료 텍스트, status-line 링크, empty-note 링크, loading-note, `.aspect .val`(슬라이더 숫자), 티켓 제목 hover, `.ticket .predicted`, `.save-btn` 텍스트, `.rec-card`의 noimg/reason 텍스트) `var(--text)`(밝은 흰색, 강조용) 또는 `var(--text-dim)`(회색, 보조용)로 교체함.

**"도형"으로 판단해서 마룬을 그대로 남긴 것**: 테두리(`border-color`), 배경 채움(`background`), box-shadow(그림자/글로우), 그리고 별점 아이콘(`.star .fill`, `.hist-item .hstars`) — 별은 유니코드 문자(★)라 엄밀히는 "글자"지만 숫자를 읽는 게 아니라 채워진 정도를 보는 아이콘/도형 역할이라고 판단해서 그대로 뒀음. 이 판단이 사용자 의도와 다르면 다시 말씀해달라고 안내할 것.

## 2026-09-12 세션 — 사용자 테스트 피드백 대량 반영 (style.css?v=38, script.js?v=26)

세션 시작 시 `index.html`, `admin.html`, `api/tmdb.js`, `PROJECT_NOTES.md`, 이미지 4개가 로컬 디스크에서
사라져 있었음(git엔 남아있었음) — 사용자 확인 받고 `git checkout`으로 복구 후 작업 시작. 이번 세션은
3장 티켓 체제(모드1/2/3, 이 문서엔 기록 안 돼 있던 이전 세션들에서 추가된 구조)가 이미 있는 상태에서
사용자가 실사용 테스트 후 남긴 피드백 목록을 한 번에 반영함.

1. **OTT 필터 추가(모드1 04단계 + 모드2 퀴즈)** — 넷플릭스/왓챠/티빙/디즈니플러스/애플TV, 복수선택 + 상관없어요.
   장르 ID를 `genreMap`으로 실시간 조회하는 것과 같은 패턴으로, provider ID도 하드코딩하지 않고
   `checkServer()`에서 `loadProviderIds()`로 `/watch/providers/movie?watch_region=KR`를 조회해서
   provider_name으로 매칭(`OTT_CHIPS`의 `matchNames`). TMDB의 실제 provider_name 표기가 예상과 다르면
   특정 OTT 칩이 안 보일 수 있음 — 이땐 `OTT_CHIPS`의 matchNames만 조정하면 됨.
2. **세부 평가 슬라이더에 설명 추가** — `aspectDescs` i18n으로 10개 항목 전부에 "높게 주시면 ~한 영화가
   추천돼요" 한 줄 설명을 슬라이더 밑에 표시 (`.aspect-desc`).
3. **검색창 X(지우기) 버튼** — 01단계 검색창, 모드3 검색창 둘 다 `.search-wrap`으로 감싸고 `.search-clear`
   버튼 추가.
4. **비슷한 영화 찾기(모드1) — 부정적 리뷰 설명 추가** — 리뷰 textarea 밑에 `reviewNegativeHint` 문구로
   "아쉬운 점을 적으면 반대 매력의 영화를 찾아준다"는 걸 설명.
5. **모드3(인생영화 분석) 대개편**:
   - 결과 맨 위에 "고르신 A, B, C의 공통점은 ○○ 장르를 좋아하신다는 점이에요" 문단 추가
     (`mode3Common`, `commonGenreNames()`로 픽한 영화들의 장르 교집합에서 이름 추출)
   - 유형 이름을 추상적 명칭("모험가" 등) 대신 **영화 캐릭터 이름**으로 표시 — `archetypes` 각 항목에
     `charName`/`charFilm`/`charBlurb` 필드 추가(ko 11개는 한국 영화 캐릭터: 고니(타짜), 김반장(극한직업),
     미자(시), 서도철(베테랑), 상우(봄날은 간다), 평경장(타짜), 미소(소공녀), 초원(말아톤),
     서태윤(살인의 추억), 송우석(변호인), 혜원(리틀 포레스트) / en 11개는 서구권 영화 캐릭터로 별도 작성).
     기존 `title` 필드(추상 명칭)는 관리자 페이지 표기용으로 남겨둠.
   - "당신과 잘 맞는 유형"도 캐릭터 이름으로 — `COMPAT_MAP`으로 유형끼리 짝을 미리 정의(대부분 상호
     참조, 신념가만 자기 자신과 매칭). 기존 `compatibility` 설명 문단은 그대로 유지하고 헤딩만 동적으로.
   - "📤 친구에게 공유해보기" 버튼 추가(`shareMode3Result()`) — **카카오 개발자 앱 키가 없어서** 카카오
     SDK 대신 `navigator.share()`(OS 공유 시트)를 사용. 모바일에서는 카카오톡이 설치돼 있으면 이 시트에
     자동으로 뜸. 지원 안 하는 브라우저(대부분 데스크톱)는 클립보드 복사로 대체. 사용자가 나중에 카카오
     개발자 센터에서 JS 키를 발급받으면 `Kakao.init()` + `Kakao.Share.sendDefault()`로 업그레이드 가능 —
     다음 세션에서 요청하면 이 부분 안내할 것.
6. **메인 티켓 화면(모드 선택 화면)에 팝콘·콜라 버튼 추가** — 기존 3장 티켓 밑에 `.mode-snacks`로
   "🍿 추천 받았던 영화"/"🥤 내 리뷰 기록" 버튼을 추가, 클릭하면 바로 appRoot로 들어가서 해당 패널이
   열림. 이미지는 사용자가 나중에 직접 넣을 예정이라 우선 이모지 텍스트로 구현.
7. **모드2(취향영화 빠르게 찾기) 대개편**:
   - 무드 칩을 단일 선택 → **복수 선택**으로 변경(`mode2State.mood`가 배열)
   - "이런 영화가 좋아요"(대중적/독립 축, `fame`)와 "이런 캐스팅이 좋아요"(`cast`) 2지선다+상관없어요
     그룹 신규 추가. `fame`은 discover의 vote_count/vote_average 기준을 다르게 조회(+"hidden"일 땐 독립
     영화 키워드도 같이 검색), `cast`는 discover가 직접 지원 안 해서 후보를 더 뽑은 뒤(`detailMovieForCard`가
     반환하는 cast 평균 popularity, `castAvgPopularity`)로 재정렬하는 방식으로 근사함.
   - OTT 그룹도 모드1과 동일한 패턴으로 추가
   - **추천 이유 개별화** — 기존엔 4장 전부 `mode2ReasonText`(고정 문구) 하나였는데, `buildMode2Reasons()`로
     카드마다 실제로 겹치는 무드/장르 + fame/cast 선택을 반영한 서로 다른 이유가 나오게 함.

로컬엔 TMDB 키가 없어서(스크래치패드가 세션마다 사라짐) 이번엔 실제 API 호출 없이 정적 파일 서버 +
브라우저 JS 콘솔로 각 함수(`buildMode2Reasons`, `commonGenreNames`, `pickArchetype`, `renderRefineStep`
등)를 직접 호출해서 로직·렌더링을 검증함. TMDB 연동이 필요한 부분(실제 검색 결과, discover 쿼리 응답)은
라이브 배포 후 다시 확인 필요.

`admin.html`도 위 변경에 맞춰 최소한으로 업데이트함 — `moodStr()`(배열 대응), OTT/대중성/캐스팅 필드,
분석 결과에 캐릭터 이름 표시 추가.

## 2026-09-12 세션 후속 — 모드3 캐릭터 설명 심화 (script.js?v=27)

바로 위 세션에서 만든 `charBlurb`(캐릭터 설명)가 1~2문장으로 너무 짧다는 피드백. 11개 캐릭터(ko) +
11개 캐릭터(en) `charBlurb`를 전부 6~8문장(8~10줄 분량)짜리 분석적인 글로 다시 씀 — 어느 영화의
어떤 인물인지, 성격의 디테일, 왜 그 장르(아키타입)를 대표하는 인물인지까지 풀어씀.

추가로 "💞 당신과 잘 맞는 유형은 OOO 유형이에요" 부분에도 **compat 캐릭터 자신의 charBlurb를 새로
끼워넣음** — 기존엔 라벨만 동적이고 그 밑 문단은 원래 유형(사용자 본인) 관점의 `compatibility` 설명
뿐이었는데, 이제 compat 캐릭터가 어느 영화의 누구고 성격이 어떤지 먼저 보여준 다음, 그 밑에 기존
`compatibility` 문단(왜 잘 맞는지)이 이어지는 구조. `runMode3Analyze()`의 verdict.innerHTML만 수정,
`mode3CharBlurbTemplate` i18n 헬퍼를 compat 쪽에도 재사용함.

로컬에서 브라우저 콘솔로 렌더링 결과 직접 확인(고니→평경장 조합 등) — 글자수 기준 문단당 390~400자로
8~10줄 분량 확보됨.

## 2026-09-13 세션 — 실사용 버그 3건 + 무드 확장 (script.js?v=28)

사용자가 실제 배포 사이트를 써보고 admin.html 스크린샷과 함께 버그를 제보함. 원인을 하나씩 찾아 고침:

1. **admin.html에서 "비슷한 영화 찾기" 카드가 전부 ☆☆☆☆☆(0)에 제목·리뷰·필터가 텅 비어 보이는 문제**
   — 원인은 정상 사용자 흐름으로는 나올 수 없는 title/rating이 없는 값이 예전에(테스트성으로 추정)
   `cinerec_reviews`에 저장돼 있었던 것. `api/save-review.js`에 **title과 rating이 없으면 400으로
   거부하는 가드**를 추가해서 앞으로는 이런 빈 값이 절대 저장되지 않게 막았고, `admin.html`의
   `loadReviews()`에서 `modeOf(r)==='similar' && !r.title && !r.rating`인 항목은 아예 화면에서 걸러내도록
   해서 기존에 이미 쌓인 빈 데이터도 안 보이게 처리함. (Upstash에 실제로 남아있는 값 자체를 지우는 건
   아니고 화면에서만 숨기는 것 — DB에서 완전히 지우려면 Upstash 콘솔에서 직접 삭제해야 함, 다음 세션에서
   필요하면 안내할 것)
2. **모드1 04단계에서 추가한 OTT 필터가 "상관없어요" 말고는 하나도 안 뜨는 문제** — `loadProviderIds()`가
   호출하는 `/watch/providers/movie` 엔드포인트가 `api/tmdb.js`의 `ALLOWED_PATH_PATTERNS` 화이트리스트에
   없어서 403으로 막히고 있었음(같은 파일에서 세션12에 이 엔드포인트를 새로 쓰기 시작했는데 화이트리스트
   추가를 빠뜨림). 패턴 추가로 해결. 배포 후 실제로 넷플릭스 등이 뜨는지 재확인 필요.
3. **`filters.ott`가 admin에서 항상 비어 보이던 것** — `api/save-review.js`가 `filters` 객체를 화이트리스트
   방식으로 재구성할 때 `ott` 필드를 옮겨담는 걸 빠뜨렸었음(세션12에서 클라이언트만 고치고 서버는 놓침).
   추가함.
4. **모드2 "지금 어떤 영화가 끌리세요?" 무드가 6개뿐이라 좁다는 피드백** — `추리하듯 단서를 좇고 싶어요`
   (미스터리+범죄), `색다른 세계관에 빠지고 싶어요`(SF), `판타지 속으로 떠나고 싶어요`, `편안하게
   힐링하고 싶어요`(애니메이션/가족), `묵직한 이야기에 빠지고 싶어요`(전쟁/역사) 5개 추가해서 총 11개로.
   전부 TMDB 표준 장르 ID라(추리처럼 키워드 근사 안 해도 됨) `MOOD_OPTIONS`에 장르ID만 추가하면 끝.
5. **04단계 독창성/연기/편집/음악 슬라이더 설명이 "높으면 ~"으로만 돼 있어 부실하다는 피드백** — "높은
   점수 → ~, 낮은 점수 → ~" 양방향 문구로 다시 씀. 독창성·연기는 원래도 코드에 진짜 반대쪽 로직이
   있었지만(disliked일 때 without_keywords, 다른 배우 보너스), **편집·연기는 disliked일 때 아무 로직도
   없었어서** `sameEditor && disliked.includes('editing')`/`sameComposer && disliked.includes('music')`에
   소폭 감점(-1, 배우 항목의 감점 패턴과 동일)을 추가해서 문구가 거짓말이 안 되게 함. (장르/주제처럼
   TMDB가 "without_crew" 파라미터를 지원하지 않아서 완전 배제는 불가능 — 감점 정도가 한계임, 문구도
   "피해서 추천"이라고만 쓰고 "절대 안 나온다"는 식으로는 안 씀)

로컬 정적 서버 + 브라우저 콘솔로 무드 11개 렌더링, 새 aspectDescs 문구, tmdb.js 화이트리스트 정규식
매칭까지 확인. admin.html의 실제 필터링 동작은 로그인/Upstash가 필요해서 라이브에서 재확인 필요.

## 2026-09-17 세션 — 로고 홈이동/갤러리 단독뷰/이전버튼/국가 선택 (style.css?v=40, script.js?v=30)

이전 세션의 OTT 수정을 사용자가 배포해봤는데도 "상관없어요"만 뜬다고 재보고 — `api/tmdb.js`의 화이트리스트
수정 코드는 이 세션 시작 시점에도 정상적으로 남아있었음(재확인함). **가장 유력한 원인은 사용자가 GitHub
웹 업로드 때 `api/` 폴더 안의 파일(특히 `api/tmdb.js`)을 빠뜨렸을 가능성** — 폴더 구조가 있는 다중 파일
업로드라 놓치기 쉬움. 이번 세션에서 OTT 로직을 국가 선택 기능과 함께 전면 재작업했으니, 재배포 시
`api/tmdb.js`를 반드시 포함해서 올리라고 강조해서 안내할 것.

1. **로고 클릭 → 첫 페이지(티켓 선택 화면)로 이동** — `.logo-img`를 `<button class="logo-btn">`로 감싸고,
   `setupIntro()`에서 `document.querySelectorAll('.logo-btn')`에 `goToModeSelect()`(기존 "다른 방식으로
   찾기"와 동일 로직, 새 함수로 분리)를 연결. 헤더 3곳(appRoot/mode2Root/mode3Root) 전부 동일 클래스라
   querySelectorAll로 한 번에 처리 — id를 쓰면 중복 id 에러가 나서 class 방식으로 감.
2. **메인 티켓 화면의 🍿/🥤 버튼 — 01~05단계 전체 대신 해당 패널만 보이게** — HTML 구조를 바꾸는 대신
   `#appRoot`에 `.gallery-only` 클래스를 토글하는 방식으로 처리(CSS로 `.steps`/`.status-line`/`.panel`을
   `display:none!important`). `enterMode()`에서 매번 클래스를 지워서 다음에 일반 티켓1로 들어갈 때 잔상이
   안 남게 함.
3. **모든 결과 화면에 "이전" 버튼 추가** — 기존엔 결과 화면(step5/mode2 결과/mode3 결과)에 완전 초기화되는
   "다시 시작/다시 고르기"만 있었음. `backToStep4`/`mode2Back`/`mode3Back`을 새로 추가 — **선택한 값은
   그대로 두고 이전 패널만 다시 보여줌**(reset 계열 버튼과 다른 점). step2/3/4의 기존 "이전" 버튼과 헤더의
   "다른 방식으로 찾기"는 그대로 둠.
4. **언어 전환(한국어/English) → 국가 선택(🇰🇷🇨🇳🇯🇵🇺🇸🇬🇧)으로 전면 교체** — 사용자가 명시적으로
   "언어 선택 없애고 국가 선택만, 국가에 따라 언어도 같이 바뀌게, 5개국(한국/중국/일본/미국/영국)"으로
   요청함(AskUserQuestion으로 "언어 유지+국가 별도 추가" vs "완전 교체" 확인 후 완전 교체 선택받음).
   **사이트엔 한/영 번역만 있어서 중국·일본을 고르면 언어는 영어로 표시돼요** — `COUNTRY_LANG = {KR:'ko',
   CN:'en', JP:'en', US:'en', GB:'en'}`. 중국어/일본어 번역이 필요하면 다음 세션에서 별도로 요청할 것
   (11개 아키타입 캐릭터 설명까지 포함하면 번역량이 상당히 큼 — 세션12~13에서 8~10줄로 늘려놔서 더 큼).
   - `.lang-switch` 버튼 2개 → `<select class="country-select">` 드롭다운으로 교체(4곳 전부: modeSelect +
     헤더 3개). 5개면 pill 버튼보다 select가 좁은 화면에서 더 안정적이라고 판단.
   - `OTT_CHIPS`(고정 5개) → `OTT_CHIP_DEFS`(전체 후보 정의) + `OTT_CHIPS_BY_COUNTRY`(국가별로 어떤 후보를
     보여줄지, key 배열)로 구조 변경. `currentOttChips()` 헬�퍼로 `OTT_CHIPS_BY_COUNTRY[state.country]`를
     꺼내 씀. 국가별 목록: KR(넷플릭스/왓챠/티빙/디즈니+/애플TV), US(넷플릭스/디즈니+/애플TV/훌루/맥스/
     프라임비디오), GB(넷플릭스/디즈니+/애플TV/프라임비디오), JP(넷플릭스/디즈니+/애플TV/프라임비디오/
     U-NEXT), CN(아이치이/유쿠/텐센트비디오). **CN/JP 신규 후보(특히 중국 3개)는 TMDB의 실제
     provider_name 표기를 실키로 확인 못 했음 — 라이브에서 안 뜨면 `matchNames` 철자를 조정해야 할 수
     있음.**
   - `loadProviderIds()`/`tmdbWatchProviders()`/추천 로직의 `watch_region` 하드코딩('KR')을 전부
     `state.country`로 교체.
   - `setCountry(code)` 신규 — 국가·언어를 함께 바꾸고, 이전 국가에서 고른 `manualFilters.ott`/
     `mode2State.ott`를 초기화(새 국가엔 없는 서비스일 수 있어서)한 뒤 `checkServer()`로 provider 목록도
     다시 받아옴. `cinerec:country`로 localStorage 저장(기존 `cinerec:lang` 대체).
   - 결과 화면 disclaimer의 "OTT 제공처(한국)" 하드코딩을 `COUNTRY_NAMES` 매핑으로 동적화.

로컬 정적 서버 + 브라우저 콘솔로: 로고 클릭 이동, gallery-only 클래스 토글, 3개 back 버튼(선택값 보존
확인 포함), 국가별 OTT 칩 목록·언어 전환·select 4개 동기화, localStorage 저장/복원까지 전부 확인함.
실제 TMDB 키가 없어서 각 국가의 provider_name 실제 매칭은 라이브에서 재확인 필요.

## 2026-09-20 세션 — OTT 결과 2단 분리 (style.css?v=41, script.js?v=33)

"사람들이 OTT를 제일 중요하게 생각할 것 같다"는 판단으로, 04단계(모드1)/모드2 퀴즈에서 OTT를
선택했을 때 결과 화면을 **"구독 중인 OTT에서 볼 수 있어요" / "(OTT명)엔 없지만, 이런 영화는
어떠세요?"** 두 섹션으로 분리함.

- 기존엔 OTT를 고르면 `with_watch_providers`로 discover 자체를 강하게 제한해서 그 OTT에 없는
  영화는 애초에 후보에 들지도 못했음. 이번엔 discover를 OTT로 제한하지 않고 넓게 뽑은 뒤,
  OTT로 제한한 조회 결과를 후보 풀에 **보강**만 하고, 각 후보의 실제 providers(이미 상세 조회
  단계에서 받아오던 값)를 기준으로 "구독 중" / "그 외" 두 그룹으로 최종 분류하는 방식으로 바꿈
  (`computeTmdbRecommendations`의 `ottParams`, `movieOnSelectedOtt()`, mode2의 `runMode2Recommend`
  동일 패턴).
- 신규 헬퍼: `selectedOttLabel(keys)`(고른 OTT 라벨을 사람이 읽을 문구로), `movieOnSelectedOtt
  (providerNames, keys)`(OTT_CHIP_DEFS의 matchNames와 대조).
- 후보 풀 크기를 OTT 선택 시 모드1은 8→12, 모드2는 (+8)로 늘려서 두 그룹 다 채울 여지를 둠.
- HTML: `#resultOttTitle`/`#resultOtherSection`(모드1), `#mode2ResultOttTitle`/
  `#mode2ResultOtherSection`(모드2) 신규 — OTT 선택 안 했으면 전부 `display:none`으로 기존과
  동일하게 단일 리스트만 보임(하위호환). "그 외" 섹션은 실제로 보여줄 영화가 있을 때만 노출.
- 티켓 카드 마크업 중복 제거 겸 `buildResultTicket()` 헬퍼로 분리(기존엔 `renderResult` 안에
  인라인으로만 있었음).
- 로컬 정적 서버(TMDB 키 없음) + 브라우저 콘솔에서 `renderResult`/mode2 렌더 부분을 목업 데이터로
  직접 호출해서 두 섹션 분리·하위호환(OTT 미선택 시 섹션 숨김) 둘 다 스크린샷으로 확인함. 실제
  TMDB 응답으로 "그 외" 그룹이 실제로 잘 채워지는지는 라이브에서 재확인 필요.

## 2026-09-20 세션 후속 — "구독 중인 OTT" 그룹이 텅 비던 버그 수정 (script.js?v=34)

배포 직후 사용자가 실제로 인셉션+넷플릭스로 테스트해보고 "구독 중인 OTT 칸엔 결과가 없고,
'그 외' 칸엔 스트리밍 정보 없는 영화만 잔뜩 나온다"고 재보고. 라이브 사이트(cinereccc.vercel.app)에서
직접 같은 흐름(인셉션 4점 + 넷플릭스)을 재현해서 원인을 찾음:

- **원인**: `computeTmdbRecommendations()`/`runMode2Recommend()`가 OTT로 제한한 보강 조회
  (`ottParams`) 결과를 그냥 기존 `results` 배열 **뒤에** append하고 있었음. 그런데 상세 조회 대상
  (`top`/`pool`)은 배열 **앞에서부터** slice(0,8~12)로 뽑았기 때문에, 인기도(popularity.desc) 1페이지가
  이미 그 슬롯을 다 채워버리면(특히 아직 스트리밍 어디에도 없는 개봉 예정/최신 대작들, 실사용
  테스트 시점엔 "스파이더맨: 브랜드 뉴 데이"/"오디세이"(놀란 신작)/"군체" 등 2026년작이 장르+인기도
  상위를 다 차지하고 있었음) OTT로 제한한 후보는 슬라이스 밖으로 밀려서 상세 조회 대상에 아예
  들지 못했음. 결과적으로 "구독 중인 OTT" 그룹은 항상 비고, "그 외" 그룹엔 스트리밍 정보가 아예
  없는(아직 개봉만 하고 스트리밍은 안 풀린) 영화만 나오는 현상이었음.
- **수정**: OTT로 제한한 조회 결과(`ottResults`)를 별도 변수로 분리해서 들고 있다가, 상세 조회 풀을
  구성할 때 **맨 앞에 우선 배치**(`ottResults.slice(0,8)` + `results.slice(0,8~10)`, dedup)하도록
  변경. 모드1(`computeTmdbRecommendations`)·모드2(`runMode2Recommend`) 둘 다 동일하게 수정. 최종
  "구독 중"/"그 외" 분류 로직(각 후보의 실시간 `providers`로 판정) 자체는 그대로 — 순서 문제만
  고쳤음.
- 라이브 API(`https://cinereccc.vercel.app/api/tmdb`)에 직접 같은 파라미터로 요청해서 검증함:
  수정 전 로직으로는 넷플릭스 매칭 0건이었던 게, 수정 후 로직으로는 16개 후보 중 8개가 실제
  넷플릭스 타이틀(터미네이터·반지의 제왕·스파이더맨 시리즈 등)로 정상 매칭됨을 확인. 로컬엔
  TMDB 키가 없어서 이 검증은 라이브 API를 직접 호출하는 방식으로 했음 — 코드 배포(재업로드) 후
  실제 화면에서 한 번 더 확인 필요.

## 2026-09-20 세션 후속 2 — OTT+다른 필터 동시 선택 시 "영화 유형"/"대중성" 무시되던 버그 (script.js?v=35)

사용자가 "OTT랑 같이 고르는 영화 유형·러닝타임·장르도 선택한 대로 반영되냐"고 물어봐서 코드를
다시 훑어보다가 발견한 버그. 장르·러닝타임·개봉연도는 문제없었지만:

- **모드1**: `ottParams`(OTT로 제한한 보강 조회용 파라미터)를 `{...params}`로 복사하는 시점이
  "영화 유형"(단편/독립영화) 처리 **이전**이었음. 그래서 단편/독립영화 키워드나 단편의 러닝타임
  상한이 `params`에 추가되기 전에 이미 복사가 끝나버려서, OTT 보강 조회에는 그 조건이 반영 안 됨.
  즉 "독립 영화 + 넷플릭스"를 같이 고르면, "구독 중인 OTT" 그룹에는 독립영화가 아닌 그냥 인기
  영화가 섞여 들어갈 수 있었음.
- **모드2**: 같은 패턴으로 `ottParams` 복사가 "대중성"(fame: 나만 안 본 것 같은/아무도 안 본 것
  같은) 처리보다 먼저 일어나서, OTT + fame='hidden'(아무도 안 본 것 같은 영화, 독립영화 키워드
  포함)을 같이 고르면 OTT 보강 조회에 그 조건이 안 들어갔음.
- **수정**: 두 함수 모두 `ottParams = {...params, ...}` 복사 시점을 해당 필터 처리 코드 **뒤로**
  옮김. 이제 OTT 보강 조회도 장르·러닝타임·개봉연도·영화 유형(모드1)/대중성(모드2)을 전부 반영한
  뒤의 `params`를 그대로 복사해서 만듦.
- 라이브 API에 직접 요청해서 이 버그가 실제로 결과에 영향을 줬는지 확인함: 같은 장르+OTT 조건에서
  독립영화 키워드 유무에 따라 전체 결과 수가 1310개→12개로 확 달라짐(당연히 반환되는 영화 목록도
  완전히 다름) — 즉 버그가 있었다면 사용자가 "독립 영화"를 골라도 OTT 그룹엔 그냥 인기영화가
  나왔을 것.

## 2026-09-20 세션 후속 3 — 모드3(인생영화 분석) 캐릭터 다양화 + UI 정리 (style.css?v=42, script.js?v=36)

사용자 피드백 5가지를 한 번에 반영:

1. **"잘 맞는 유형이 너무 한국 영화에 국한된다"** — 원인은 사용자가 고른 영화의 국적이 아니라
   (애초에 장르 기반 매칭이라 국적은 안 봄), 11개 유형(archetype)마다 캐릭터가 **고정 1명**이었고
   그 1명이 전부 한국 영화 캐릭터였던 것. 각 유형에 해외 영화 캐릭터를 하나씩 추가해서
   `chars:[{name,film,blurb}, {name,film,blurb}]` 배열 구조로 바꿈 — 기존 한국 캐릭터(고니/타짜,
   김반장/극한직업, 미자/시, 서도철/베테랑, 상우/봄날은 간다, 평경장/타짜, 미소/소공녀, 초원/말아톤,
   서태윤/살인의 추억, 송우석/변호인, 혜원/리틀 포레스트) + 세션12에서 이미 en 버전에 있던 것과
   동일한 해외 캐릭터(인디아나 존스, 지니, 아멜리에, 이단 헌트, 잭 도슨, 셜록 홈즈, 월터 미티,
   포레스트 검프, 로버트 랭던, 윌리엄 월레스, 크리스토퍼 매캔들리스)를 새로 한국어로 집필해서 추가.
   `pickArchetypeChar(chars, seedNum)`으로 고르신 영화 id 합을 시드 삼아 결정적으로 하나를 골라서
   (같은 영화를 다시 고르면 같은 결과가 나옴) 매번 한국 캐릭터로만 고정되지 않게 함. en 로케일은
   기존 캐릭터 그대로 `chars:[{...}]` 1개짜리 배열로만 구조 변경(내용 변경 없음).
2. **"분석이 너무 단순하다"** — 기존 8개 문장 내외였던 캐릭터 분석(charBlurb)을 한국 캐릭터 11개
   전부 2~3문장씩 추가해서 늘렸고, 새로 쓴 해외 캐릭터 11개도 비슷한 분량(9~10문장)으로 집필.
   personality/strength/lifeGoal 필드는 이번엔 손대지 않음(분량 문제로 범위 밖으로 판단, 필요하면
   다음 세션에서 추가 요청 받을 것).
3. **각 항목 줄간격/가독성** — `.mode3-trait`마다 `border-top` 구분선 + 위쪽 여백을 넉넉히 줘서
   섹션이 시각적으로 분리되게 함. `.mode3-char-blurb`를 배경색 있는 카드로 바꿔서 본문과 구분.
   라벨을 `<b>` 대신 `.mode3-trait-label`(block) 전용 클래스로 분리.
4. **"나와 잘 맞을 영화" → "당신에게 추천하는 영화"** — `mode3RecLabel` i18n 값과 index.html
   정적 텍스트 둘 다 수정. en도 "Your movie match" → "Recommended for You"로 정리.
5. **"💞 당신과 잘 맞는 유형은 / 고니 / 유형이에요"처럼 3줄로 깨지던 버그** — 원인은 CSS
   `.mode3-trait b{display:block}` 규칙이 라벨 감싸는 `<b>`뿐 아니라, 그 **안에 중첩된**
   `<b>${charName}</b>`에도 그대로 적용되면서 캐릭터 이름이 강제로 줄바꿈되던 것. `mode3VerdictTitle`/
   `mode3CharBlurbTemplate`/`mode3LabelCompat` 템플릿의 강조용 `<b>`를 전부 `<span class="mode3-hl">`
   (inline, `color:var(--accent)`)로 바꾸고, `.mode3-trait b{display:block}` 규칙 자체를 삭제해서
   근본 원인을 없앰.

로컬 정적 서버 + 브라우저 콘솔에서 `runMode3Analyze()`를 다른 시드(고른 영화 id 합)로 여러 번
직접 호출해서: 캐릭터가 실제로 한국/해외로 갈리는지(홀짝에 따라 고니↔인디아나 존스, 셜록
홈즈로 나뉘는 것 확인), "당신과 잘 맞는 유형은 X 유형이에요"가 한 줄로 정상 표시되는지, 섹션
구분·긴 텍스트 렌더링까지 스크린샷으로 확인함.

## 2026-09-20 세션 후속 4 — 중국/일본 국가 선택지 제거 (script.js?v=37)

번역(zh/ja) 안 하기로 하면서, "다음에 뭐 추가하면 좋겠냐" 질문에 중국/일본 번역 부재와 OTT
provider 매칭 미검증을 얘기했더니 사용자가 "그럼 그냥 빼자"고 결정함.

- index.html의 국가 선택 `<select>` 4곳(모드 선택 화면 + 앱/모드2/모드3 헤더 각각)에서
  `<option value="CN">`/`<option value="JP">` 제거.
- script.js `COUNTRY_LANG`/`COUNTRY_NAMES`에서 CN/JP 항목 삭제.
- `OTT_CHIPS_BY_COUNTRY`에서 JP/CN 항목 삭제. 그 두 국가에서만 쓰이던 OTT 후보(U-NEXT/아이치이/
  유쿠/텐센트)가 완전히 안 쓰이게 돼서 `OTT_CHIP_DEFS`와 ko/en `ottOption*` i18n 라벨까지 같이 삭제
  (죽은 코드 안 남기려고).
- 기존에 CN/JP를 골랐던 사용자의 `localStorage`(`cinerec:country`)는 그대로 남아있어도 문제없음 —
  `setCountry()`/앱 시작 시 복원 로직이 `COUNTRY_LANG[code]` 존재 여부로 가드돼 있어서, 이제 없는
  코드면 자동으로 기본값(KR/한국어)으로 무시됨. 로컬에서 `localStorage`에 `JP`를 직접 넣고 새로고침해서
  KR로 안전하게 폴백되는 것 확인함.
- 국가 선택지가 KR/US/GB 3개로 줄어듦. **중국어/일본어 번역 관련 미완성 항목은 이걸로 종료** —
  아래 미완성 목록에서도 제거함.

로컬 정적 서버에서 select 4개 전부 옵션이 3개(KR/US/GB)인 것, localStorage 폴백까지 확인함.

## 아직 사용자가 안 물어봤지만 미완성/논의만 된 것

- **Google 로그인** — 사용자가 물어봤지만 "지금은 로그인 없이 되는 걸로" 방향으로 선회해서(=8번, 9번 갤러리 기능) 보류 상태. 다시 나올 수 있는 주제.
- **Netlify 배포** — 코드/문서에 옵션으로 남아있지만 실제로 쓴 적 없음, Vercel만 사용 중.
- **카카오톡 진짜 연동** — 위 5번 참고. 카카오 개발자 앱 키를 발급받으면 업그레이드 가능.

## 로컬 개발 서버 (테스트용, repo에는 없음)

이 세션에서 로컬 테스트용 Python 서버를 스크래치패드에 만들어서 씀:
`TMDB_API_KEY='<키>' ADMIN_PASSWORD='test1234' python3 local_server.py <cinelog폴더경로> 8000`
— TMDB 프록시 + `/api/save-review`, `/api/list-reviews`, `/api/admin-login`, `/api/admin-logout`를
**로컬 JSON 파일 + 인메모리 세션**으로 흉내낸 목업. 실제 배포 코드(Upstash 사용)와는 다름,
새 세션에서 필요하면 새로 만들어야 함 (이전 세션의 스크래치패드는 세션 종료 후 사라짐).

## 사용자 커뮤니케이션 스타일 관련 메모

- 모든 대화가 한국어로 진행됨, 답변도 한국어로.
- 사용자가 기술적으로 아주 능숙하진 않음 (GitHub 웹 업로드로 폴더 구조가 자주 꼬였음, git/CLI 개념
  설명이 필요했음, "Upstash가 뭐야?" 같은 기초 질문도 함) — **쉬운 비유와 단계별 설명이 잘 먹힘.**
- 화면 변경할 때마다 로컬 서버로 실제 렌더링 확인 후 보고하는 패턴을 계속 유지해왔음.
- CSS/JS 수정할 때마다 `style.css?v=N`, `script.js?v=N` 쿼리 버전을 올려야 브라우저 캐시가
  안 걸림 (이 프리뷰 브라우저 도구가 캐시를 꽤 공격적으로 함).
